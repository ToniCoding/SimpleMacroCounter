security:
    password_hashers:
        App\Entity\User:
            algorithm: auto

    providers:
        app_user_provider:
            entity:
                class: App\Entity\User
                property: username

    firewalls:
        generate_jwt:
            pattern: ^/api/generate-jwt$
            stateless: false
            security: true

        api:
            pattern: ^/api
            stateless: true
            jwt: ~

        main:
            pattern: ^/
            stateless: false
            form_login:
                login_path: app_login
                check_path: app_login
                enable_csrf: true
                default_target_path: home
            logout:
                path: app_logout
                target: /login

    access_control:
        - { path: ^/generate-jwt, roles: IS_AUTHENTICATED_FULLY }
        - { path: ^/api, roles: IS_AUTHENTICATED_FULLY }
        - { path: ^/(login|register), roles: PUBLIC_ACCESS }
        - { path: ^/, roles: ROLE_USER }
